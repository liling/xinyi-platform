services:
  xinyi-platform:
    image: ghcr.io/liling/xinyi-platform:latest
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    networks:
      - default
      - hindsight_default

networks:
  hindsight_default:
    external: true
